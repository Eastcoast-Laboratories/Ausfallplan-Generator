# Fix cache permissions every 5 minutes and clear old cache files
*/5 * * * * root bash /var/www/html/bin/clear-cache.sh > /proc/1/fd/1 2>&1

# Also make console command available
# Usage: docker compose -f docker/docker-compose.yml exec app bin/cake cache_clear
